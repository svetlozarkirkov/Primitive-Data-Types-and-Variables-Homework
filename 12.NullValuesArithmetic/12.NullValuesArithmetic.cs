using System;

namespace NullValuesArithmetic
{
    class NullValuesArithmetic
    {
        static void Main()
        {
            //int first = null;
            //double second = null;
            //Console.WriteLine("{0}, {1}",first,second);
            //Cannot convert null to these types because they are non-nullable.
        }
    }
}
